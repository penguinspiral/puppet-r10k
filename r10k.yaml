sources:
  main:
    # r10k control repository
    remote: 'https://github.com/penguinspiral/puppet-control-repo.git'

    # Install directory
    basedir: '/etc/puppet/code/environments'

    # Omit Gitflow 'develop' branch
    ignore_branch_prefixes:
      - 'develop'

forge:
  # IPv4 & IPv6 domain
  baseurl: 'https://forgeapi-cdn.puppet.com'
